/** @type {import('next').NextConfig} */ 
const nextConfig = {
    images: {
        domains: [
            'images.unsplash.com', 
            'cms.travelworld.nl',
              'res.cloudinary.com'
        ],
    },
};

export default nextConfig;